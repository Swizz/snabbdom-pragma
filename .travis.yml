language: node_js
node_js:
  - '6' # lts

cache:
  yarn: true

script:
  - yarn run lint
  - yarn run test:prebuild
  - yarn run build
  - yarn run test:postbuild

branches:
  only:
    - feature/travis-ci

# deploy:
#   - provider: npm
#     email: 'xxxx'
#     api_key: 'xxxx'
#     skip_cleanup: true
#     on:
#       tags: true
#       branch: master
#
#  - provider: script
#    script:
#      - git add 'dist/'
#      - git commit -m '[ci skip] Build '$TRAVIS_TAG
#    skip_cleanup: true
#    on:
#      tags: true
#      branch: master
